import React from "react";

const ImageToggleOnMouseOver = ({ primaryImg, secondaryImg }) => {
  return (
    <img onMouseOver={() => {}} onMouseOut={() => {}} src={primaryImg} alt="" />
  );
};

export default ImageToggleOnMouseOver;
